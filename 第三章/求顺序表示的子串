PSeqString subStr _ seq( PSeqString s , int i , int j ){

    PSegString s1 ;
     int k ;
     s1=createNullStr_ seq( j ); 
     if (s1== NULL ) return NULL ; 
     if ( i >0 && i <= S -> n && j >0){
                   if ( s -> n < i + j -1) j = S -> n - i + 1;

                    for ( k =0; k < j ; k ++)
                            s1-> c [ k ]= S -> c [ i + k -1];
                    s1-> n = j ;
                   }

    return sl ;
}
